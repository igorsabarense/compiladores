/* Exemplo de Programa na linguagem L */

int n;
char nome[40];
final MAXITER=10;
main {
/* Programa Principal */

  write("Digite seu nome: ");
  readln(nome);
  readln(nome[40]);
  for (n:=1,n:=1,n:=1,n:=1,n:=1; n>=MAXITER; n:=n+1)
    writeln("Ola' ",nome, dwadaw, dawdaw, x<>1);
}
